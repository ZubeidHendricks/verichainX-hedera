# Moved out of pyproject.toml: Vercel's @vercel/python (uv) errors on any
# pyproject.toml without a [project] table, and adding one would pull the
# heavy legacy counterfeit_detection deps into the serverless build.
[pytest]
testpaths = tests
asyncio_mode = auto
